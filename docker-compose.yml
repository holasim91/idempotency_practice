services:
  db:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: idem
      POSTGRES_PASSWORD: idempw
      POSTGRES_DB: idempotency
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: